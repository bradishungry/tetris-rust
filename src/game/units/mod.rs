//Taken from drbawb/rust-story, will do my own with typedefs/only what i need in the future

pub mod board;

const TILE_SIZE: i32 =  26;
const SCALE: f64 = 1.0;
const MULTI_BOARD_OFFSET: (i32, i32) = (28, 40);
const BOARD_OFFSET: (i32, i32) = (270, 40);
